# Demo Notes

- Use `sample_topology.json` as a small architecture review fixture.
- Run the CLI with `--focus identity` to see the widest blast radius.
- In a real team setting, add latency budgets, datastore dependencies, and rollout policies next.
