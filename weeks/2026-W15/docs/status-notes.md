# Repo Health Status Notes

The report now labels each repo with a simple status:

- **healthy**: clean working tree, no test or lint failures, and no scan errors.
- **needs-attention**: any dirty state, failing checks, or scan errors.

This makes it easy to spot which repos require follow-up without scanning every signal.
